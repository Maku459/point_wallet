@docs/knowledge.md

# 実装フロー

- 実装をするときは、基本的に必ず新しいブランチを切り、PRを作成するところまで行うこと。
- 前の実装の続きと考えられるときは、ブランチを切るかどうかユーザーに確認すること。

# 記録

- セッションを始めるときは `docs/knowledge.md` に目を通す（上の `@` で自動読み込みされる）。
  過去の実装で分かったこと・触るときの注意がまとまっている。
- 「いつ何をしたか」を辿りたいときは `docs/worklog.md` を読む（自動読み込みはしない）。
- 実装が終わったら、コミットする前に次の 2 つを更新すること。
  - `docs/worklog.md` の一番上に 1 件足す（日付・見出し・PR 番号・やったこと・コミットハッシュ）。
  - 次に同じところを触る人が知らないと壊すことがあれば `docs/knowledge.md` に足す。
    1 回きりの作業手順ではなく、繰り返し効く約束と落とし穴だけを書く。
- 役割を混ぜないこと。`README.md` は「アプリの仕様」、`docs/knowledge.md` は「触るときの注意」、
  `docs/worklog.md` は「いつ何をしたか」。仕様が変わったら README を直す。
